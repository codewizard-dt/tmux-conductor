FROM node:22-alpine
RUN apk add --no-cache bash tmux
WORKDIR /app
COPY package.json package-lock.json ./
RUN apk add --no-cache --virtual .build-deps python3 make g++ \
 && npm install \
 && apk del .build-deps
ENV HOST=0.0.0.0
CMD ["node", "--watch", "--experimental-strip-types", "index.ts"]
