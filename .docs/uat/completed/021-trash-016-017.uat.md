# UAT: Trash Tasks 016 and 017

> **Source task**: [`.docs/tasks/021-trash-016-017.md`](../tasks/021-trash-016-017.md)
> **Generated**: 2026-06-06

---

## Prerequisites

- [ ] Working directory is the repo root (`/Users/davidtaylor/Repositories/tmux-conductor` or equivalent)
- [ ] Task 021 steps have been executed (files moved, README updated)

---

## Shell / File Tests

### UAT-SHELL-001: Task 016 is absent from `.docs/tasks/`

- **Description**: Verify `016-ensure-container-up.md` no longer exists at the active tasks root.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  ls .docs/tasks/016-ensure-container-up.md 2>&1
  ```
- **Expected Result**: Output contains `No such file or directory` — the file is NOT present at `.docs/tasks/016-ensure-container-up.md`.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-002: Task 017 is absent from `.docs/tasks/`

- **Description**: Verify `017-command-center-dashboard.md` no longer exists at the active tasks root.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  ls .docs/tasks/017-command-center-dashboard.md 2>&1
  ```
- **Expected Result**: Output contains `No such file or directory` — the file is NOT present at `.docs/tasks/017-command-center-dashboard.md`.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-003: Task 016 is present in `.docs/tasks/trashed/`

- **Description**: Verify `016-ensure-container-up.md` was moved to the trashed directory.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  ls .docs/tasks/trashed/016-ensure-container-up.md
  ```
- **Expected Result**: Command exits 0 and prints `.docs/tasks/trashed/016-ensure-container-up.md` — the file exists in `trashed/`.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-004: Task 017 is present in `.docs/tasks/trashed/`

- **Description**: Verify `017-command-center-dashboard.md` was moved to the trashed directory.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  ls .docs/tasks/trashed/017-command-center-dashboard.md
  ```
- **Expected Result**: Command exits 0 and prints `.docs/tasks/trashed/017-command-center-dashboard.md` — the file exists in `trashed/`.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-005: README Active Tasks table has no row for task 016

- **Description**: Verify the Active Tasks table in `.docs/tasks/README.md` no longer mentions task 016.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  grep -c '| 016 ' .docs/tasks/README.md
  ```
- **Expected Result**: Output is `0` — no row for 016 exists in the README.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-006: README Active Tasks table has no row for task 017

- **Description**: Verify the Active Tasks table in `.docs/tasks/README.md` no longer mentions task 017.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  grep -c '| 017 ' .docs/tasks/README.md
  ```
- **Expected Result**: Output is `0` — no row for 017 exists in the README.
- [x] Pass <!-- 2026-06-06 -->

### UAT-SHELL-007: git tracks the moves (not deletions)

- **Description**: Verify git recorded the operation as a rename, not a delete + untracked file. This confirms `git mv` was used rather than plain `mv`.
- **Steps**:
  1. Run the command below from the repo root.
- **Command**:
  ```bash
  git log --diff-filter=R --name-status --oneline -- '.docs/tasks/trashed/016-ensure-container-up.md' '.docs/tasks/trashed/017-command-center-dashboard.md'
  ```
- **Expected Result**: Output contains at least one commit line and rename entries (`R...`) for both `016-ensure-container-up.md` and `017-command-center-dashboard.md`, confirming git tracks them as renames from `.docs/tasks/` to `.docs/tasks/trashed/`.
- [x] Pass <!-- 2026-06-06 -->
