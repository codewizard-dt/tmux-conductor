# Wiki Log

Append-only record of wiki operations — ingests, queries filed back, lint passes, scaffolding. **Never edit existing entries**; only append new ones at the bottom.

Entry format (consistent prefix keeps the log greppable — `grep "^## \[" log.md | tail -5`):

```
## [YYYY-MM-DD] <op> | <subject>
1–3 sentences on what happened.
```

Operations: `scaffold`, `ingest`, `query`, `lint`, `decision`, `task`, `bug`, `requirement`, `roadmap`.

---

## [2026-06-12] uat | UAT-001 UAT: Skill detection — backend scanner + dashboard surfacing
Generated UAT-001 for TASK-001 with 11 test cases: 5 API happy-path/shape tests, 3 edge-case tests (404, empty project, wrong agent), and 5 UI tests covering the AgentDetailModal skills section, badge rendering, and click-to-enqueue flow.

## [2026-06-12] task | TASK-001 Skill detection — backend scanner + dashboard surfacing
Created TASK-001: scan ~/.claude/skills (user-global) and per-agent <workdir>/.claude/skills (project), expose GET /skills and GET /agents/:agent/skills in the Fastify backend, and render available skills inside AgentDetailModal with click-to-enqueue support.

## [2026-06-12] roadmap | ROADMAP-001 SQLite data layer, Projects, and recurring schedules
Created ROADMAP-001 with 5 phases / 19 inline items: migrate tasks.txt + conductor.conf arrays to SQLite (better-sqlite3 + sqlite3 CLI), add Projects as a top-level resource with per-project agent spawning, and add interval-based schedules fired by the backend. Design reference: plan file from the 2026-06-12 planning session. Added to roadmaps index.

## [2026-06-12] task | TASK-002 Add better-sqlite3 dependency, data/ gitignore entry, and DB_PATH setting
Created TASK-002: prerequisite config foundation for ROADMAP-001 SQLite migration — install better-sqlite3 + @types/better-sqlite3 in backend/package.json, add data/ to .gitignore, and add DB_PATH to conductor.conf.

## [2026-06-12] task | TASK-003 Create backend/db.ts — schema migrations and typed query helpers
Created TASK-003: full SQLite data module — openDb with WAL+pragmas, schema migrations for 6 tables (meta/projects/agents/bg_processes/tasks/schedules), typed CRUD helpers, atomic pop, and seedFromLegacy one-time import.

## [2026-06-12] task | TASK-004 Verify Phase 1 foundation — seed import correctness and restart idempotency
Created TASK-004: wire openDb into backend/index.ts startup, expose _confPath on ConductorConf, and write UAT test cases for seed correctness and idempotency.

## [2026-06-12] task | TASK-005 Rewrite agent and bg-process routes to be DB-backed with spawnAgentWindow helper
Created TASK-005: replace conf-splice agent/bg CRUD with DB helpers, extract shared spawnAgentWindow(), update buildSnapshot() to use listAgents(db).

## [2026-06-12] task | TASK-006 Replace index-based queue endpoints with ID-based /api/tasks routes and SSE events
Created TASK-006: POST/DELETE/PUT /api/tasks + jump-head endpoint, update GET /api/queue/:agent to return task objects, broadcast task-added/task-removed SSE.

## [2026-06-12] task | TASK-007 Add /api/projects CRUD and POST /api/projects/:id/agents with auto-naming
Created TASK-007: full /api/projects resource with force-delete guard, plus spawn-agent-for-project endpoint with nextAgentName auto-fill and project workdir/launchCmd pre-fill.

## [2026-06-12] task | TASK-008 Add /api/schedules CRUD and the scheduler tick loop
Created TASK-008: /api/schedules CRUD + setInterval(5000) tick firing dueSchedules via fireSchedule, broadcasting schedule-fired + task-added SSE.

## [2026-06-12] task | TASK-009 Create scripts/lib/db.sh — sql wrapper, load_agents, load_bg, pop_task_sql
Created TASK-009: shared SQLite helper library for shell scripts — sql() wrapper, load_agents/load_bg array loaders, pop_task_sql atomic DELETE…RETURNING.

## [2026-06-12] task | TASK-010 Rewrite monitor.sh pop_task as atomic SQL pop and move_to_backlog as status flip
Created TASK-010: migrate monitor.sh queue ops to SQLite — pop_task_sql call, UPDATE status='backlog', load_agents inside poll loop for live agent pickup.

## [2026-06-12] uat | UAT-004 UAT: Phase 1 foundation — SQLite startup, seed import, and idempotency
Generated UAT-004 for TASK-004 with 8 test cases: 2 edge-case config/gitignore checks, 4 API/runtime tests (startup, schema init, agent seed, task seed), and 2 edge cases (idempotency restart, concurrent start).

## [2026-06-12] uat | UAT-004 passed (auto)
Task TASK-004 marked done. All 9 tests passed: DB_PATH in conf, data/ gitignored, backend startup + DB creation, meta table schema_version/legacy_import, agent seed count, task seed count, restart idempotency, better-sqlite3 binary loadable, concurrent-start atomicity. Fixed test URL from /healthz to /api/healthz.

## [2026-06-12] roadmap | ROADMAP-002 Hosted portal with Google OAuth, device relay, and installer
Created ROADMAP-002 with 5 phases / 21 inline items: DO App Platform portal (Google OIDC + email allowlist, Postgres for identity only), outbound WebSocket relay from the local daemon (pairing-code device auth, user-first connection registry), curl|bash install.sh (prereqs, SQLite init, hooks, daemon service), and frontend relay mode with onboarding. Phase 1 depends on ROADMAP-001 Phase 1. Design reference: plan file from the 2026-06-12 planning session (relay protocol, DDL, route table, security checklist). Added to roadmaps index.

## [2026-06-12] uat | UAT-005 UAT: DB-backed agent and bg-process routes with spawnAgentWindow helper
Generated UAT-005 for TASK-005 with 11 test cases: 8 API tests (GET /api/agents, GET /api/status sourcing, POST/DELETE validation and 409/404 responses for agents and bg-processes) and 3 edge-case tests (DB persistence across restart, conf-splice regression check, seeded agent count consistency).

## [2026-06-12] uat | UAT-006 UAT: ID-based /api/tasks routes and SSE events
Generated UAT-006 for TASK-006 with 13 test cases: 11 API tests (POST happy path global + scoped, validation 400s, DELETE by id, PUT reorder, POST jump-head, GET queue returning objects) and 2 edge cases (head placement ordering, hard delete confirmation).

## [2026-06-12] task | TASK-011 Immediate dispatch for tasks enqueued to an idle agent with an empty queue
Created task TASK-011: backend fast-path on the task-add routes — when the target agent is idle with no pending tasks, mark busy, log to dispatch.jsonl, and send-keys immediately instead of waiting up to POLL_INTERVAL for the monitor poll.

## [2026-06-12] uat | UAT-011 UAT: Immediate dispatch for tasks enqueued to an idle agent with an empty queue
Generated UAT-011 for TASK-011 with 10 test cases: 2 fast-path API happy paths (legacy queue route + DB tasks route), 1 dispatch.jsonl record verification, 2 slow-path API tests (busy agent, non-empty queue), 2 UI tests (fast-path hint + slow-path queue update), and 3 edge-case tests (no double-dispatch, dispatched:false for global task, state-file-before-send-keys ordering guarantee).

## [2026-06-12] task | TASK-012 Replace tail polling with SSE push for terminal output
Created task TASK-012: replace the LogTail.tsx interval poll (100ms modal / 500ms interact / 3s card) with a backend tailPollLoop at 200ms that diffs capture-pane output and broadcasts terminal-output events through the existing GET /events SSE stream; the <pre> renderer and POST /keys interact path are unchanged.

## [2026-06-12] uat | UAT-009 UAT: Create scripts/lib/db.sh — sql wrapper, load_agents, load_bg, pop_task_sql
Generated UAT-009 for TASK-009 with 14 test cases covering the db.sh shell helper library: 7 CLI tests (syntax/source check, sql() execution, \x1f separator verification via cat -v, sql_one first-row, load_agents arrays, AGENT_BG LEFT JOIN, load_bg arrays), 3 pop ordering tests (scoped-first pop, full scoped→project→global drain, unscoped-agent global-only pop), and 4 edge cases (empty-queue rc=1, empty-table arrays, the known non-fatal macOS realpath -m quirk, and pop atomicity / no double-dispatch). All commands were smoke-tested against a seeded scratch DB and produce the documented output.

## [2026-06-12] uat | UAT-010 UAT: Rewrite monitor.sh pop_task as atomic SQL pop and move_to_backlog as status flip
Generated UAT-010 for TASK-010 with 8 test cases covering the monitor.sh SQLite migration: 2 static gates (bash -n syntax, db.sh sourced + zero TASK_QUEUE/tasks.txt references), 2 pop_task tests (atomic DELETE…RETURNING that removes the row, full scoped→project→global drain + empty rc=1), 2 move_to_backlog tests (status='backlog' flip scoped to one agent, unknown-agent no-op guard), and 2 load_agents-in-loop tests (a mid-tick inserted agent visible on the next load_agents call, plus a static check that load_agents/load_bg sit at the loop top after the ITER increment). Behavioral tests source the real scripts/lib/db.sh with the exact monitor.sh wrapper bodies (monitor.sh can't be sourced — it runs the poll loop at top level) against seeded scratch DBs under ./tmp/uat-monitor/; all 8 commands were executed during generation and produce the documented output.

## [2026-06-12] uat | UAT-014 UAT: Rewrite add-task.sh to insert directly via sqlite3
Generated UAT-014 for TASK-014 with 6 test cases covering the add-task.sh SQLite-insert rewrite: 1 static gate (bash -n syntax), 1 scoped-insert test (CWD basename=alpha resolves to agent_id=1, row written with position=1.0, status='queued', source='manual', project_id NULL), 1 global-fallback test (unknown basename → stderr warning, agent_id/project_id NULL, still queued), 1 tail-ordering test (two successive inserts land at position 1.0 then 2.0 via COALESCE(MAX(position),0)+1.0 across the queued set), and 2 edge cases (empty-args usage guard exits rc=1 with no row written; single-quote command text round-trips via quote-doubling with no SQL error). All commands were executed during generation against a seeded scratch DB under ./tmp/uat-add-task/ and produce the documented output.

## [2026-06-12] uat | UAT-013 UAT: Migrate conductor.sh, spawn.sh, teardown.sh, broadcast.sh to DB-loaded agent lists
Generated UAT-013 for TASK-013 with 10 test cases covering the four lifecycle scripts' migration off conductor.conf AGENTS/BG_PROCESSES arrays onto scripts/lib/db.sh load_agents/load_bg: 4 syntax gates (bash -n per script), 3 static-content checks (each script sources lib/db.sh + calls the right loaders — conductor/spawn/teardown call load_agents+load_bg, broadcast calls only load_agents; zero residual ${AGENTS[ / ${BG_PROCESSES[ references; positive iteration of AGENT_NAMES[]/BG_NAMES[]), 2 functional loader tests against seeded scratch DBs under ./tmp/uat-013/ (load_agents populates name-ordered AGENT_NAMES[] with AGENT_DIRS/AGENT_CMDS and the AGENT_BG LEFT JOIN link resolving to the bg name or empty string; load_bg populates name-ordered BG_NAMES[] with BG_DIRS/BG_CMDS), and 1 edge case (empty agent+bg tables yield empty arrays under set -euo pipefail with no spurious entries). All 10 commands were executed during generation and produce the documented output.

## [2026-06-12] uat | UAT-014 passed (auto)
Ran all 6 UAT-014 tests headless against seeded scratch DBs under ./tmp/uat-add-task/ — all passed: bash -n syntax gate, scoped insert (agent_id=1, position=1.0, queued/manual, project_id NULL), global fallback for unknown basename (stderr warning + NULL agent_id/project_id, still queued), tail-position ordering across two inserts (1.0 then 2.0), empty-args usage guard (rc=1, no row), and single-quote command round-trip via quote-doubling. Task TASK-014 marked done; UAT-014 and TASK-014 moved to completed/, roadmap ROADMAP-001 Phase 3 TASK-014 checkbox flipped.

## [2026-06-12] uat | UAT-012 UAT: Replace tail polling with SSE push for terminal output
Generated UAT-012 for TASK-012 with 13 test cases covering the SSE terminal-output refactor: 6 API tests (one-shot /agents/:agent/tail backfill still returns pane text; 404 for unknown agent; windowPresent:false for a DB agent with no tmux window; GET /events emits event: terminal-output on pane change; payload contract {agent, text, lines:100} as a full snapshot; diff-suppression — a static pane does not re-broadcast every 200 ms cycle), 4 UI tests (card-view renders + updates live with only one tail backfill request and no recurring polling; terminal-output events drive the <pre> via the EventStream; modal detail view updates with pollMs removed; Direct Input keystrokes echo back through SSE), and 3 edge cases (single shared ref-counted EventSource across many LogTail instances, 15 s heartbeat keeps an idle stream alive, backend+frontend tsc gate green). Contracts researched from backend/index.ts (tailPollLoop, broadcastSSE, /events, /agents/:agent/tail) and frontend useSSE.ts + LogTail.tsx.

## [2026-06-12] uat | UAT-015 UAT: Frontend ID-based tasks — lib/api helpers, TaskList per-row delete + jump-to-head, AddTaskForm/AgentList wiring
Generated UAT-015 for TASK-015 with 19 test cases covering the frontend string[]→Task[] migration: 15 auto-runnable static-content tests (make typecheck-frontend + make typecheck gates; Task interface shape; the four ID-based helpers addTask/deleteTask/reorderTasks/jumpTaskToHead exist and target the correct /api/tasks routes+methods; addTask 200-dispatched vs 201-Task branch; no legacy /queue/ refs; TaskList keyed by task.id with per-row delete + ↑ head jump-to-head controls and id-array reorder; AddTaskForm posts via addTask and surfaces the returned Task; AgentList wires Task[] state at both render paths; no residual tasks: string[]) plus 4 human/Playwright-verification UI tests (delete removes the row, ↑ head moves to front and persists, drag reorder persists, busy-append vs idle-immediate-dispatch). Contracts researched from frontend/src/lib/api.ts, TaskList.tsx, AddTaskForm.tsx, AgentList.tsx and backend/index.ts /tasks routes.

## [2026-06-12] task | TASK-015 Frontend ID-based tasks — UAT-015 passed, task completed
Ran /uat-auto on UAT-015: all 15 static tests passed (make typecheck + make typecheck-frontend clean; lib/api.ts Task interface + addTask/deleteTask/reorderTasks/jumpTaskToHead helpers on ID-based /api/tasks routes; TaskList per-row delete + ↑ head + id-keyed drag reorder; AddTaskForm/AgentList wiring; no legacy /queue or string[] residue). The 4 UAT-UI tests are human/Playwright-only and recorded as expected auto-judge fails. Flipped UAT-015→passed and TASK-015→done, moved both to completed/, removed their index rows, and checked off TASK-015 in ROADMAP-001 Phase 4.

## [2026-06-12] task | TASK-019 Parameterize daemon launchd plist and add a systemd user-unit template
Created TASK-019 (ROADMAP-002 Phase 1): replace the plist's two hardcoded /Users/davidtaylor log paths with __LOG_PATH__ and prepend __NODE_PATH__ to the launchd PATH (nvm node is invisible to launchd, causing crash-loops), and add a new daemon/tmux-conductor-daemon.service.in systemd user unit reusing the same __REPO_ROOT__/__LOG_PATH__/__NODE_PATH__ placeholders (runs tsx daemon/index.ts, Restart=always, systemctl --user enable --now). Template/placeholder authoring only — the renderer in bin/conductor daemon install is the separate downstream TASK-019 blocks: TASK-020. Depends on nothing active (ROADMAP-001 Phase 1 done). Added to tasks index.

## [2026-06-12] task | TASK-020 Sync bin/conductor daemon install to the rendered-template + bootout/bootstrap flow
Created TASK-020 (ROADMAP-002 Phase 1): rewrite the daemon install subcommand as the single canonical service installer. It substitutes all three placeholders (__REPO_ROOT__, __LOG_PATH__=$HOME/.local/share/tmux-conductor/daemon.log, __NODE_PATH__=$(dirname $(command -v node))), then on macOS renders to ~/Library/LaunchAgents and loads idempotently via launchctl bootout||true + bootstrap gui/$(id -u) (replacing deprecated launchctl load), or on Linux renders daemon/tmux-conductor-daemon.service.in to ~/.config/systemd/user and runs systemctl --user daemon-reload + enable --now plus a loginctl enable-linger tip. Help text kept in sync. DEPENDS ON TASK-019 (needs the placeholders + .service.in template); BLOCKS TASK-022 (install.sh must call this rather than re-implement rendering). Added to tasks index.

## [2026-06-12] task | TASK-022 Verify install.sh end-to-end into a scratch CONDUCTOR_HOME on macOS
Created TASK-022 (ROADMAP-002 Phase 1, terminal task): a verification task whose acceptance criteria ARE the test steps. Runs install.sh non-interactively into a repo-local scratch CONDUCTOR_HOME (./tmp/conductor-install-test), confirms the rendered daemon launchd job is healthy via curl --unix-socket .../healthz, re-runs to prove idempotency (no clobber, ff-only pull, no duplicate service, clean bootout/bootstrap re-register, db:migrate no-op), then fully tears down (launchctl bootout the scratch daemon, rm the scratch home + logs, restore/remove the test symlink) while proving the developer's real daemon, identity files, and ~/.local/bin/conductor symlink are left untouched. A skipped/degraded pairing step is a PASS (pair CLI not built until Phase 3); the Linux/systemd path is review-only and out of scope. DEPENDS ON TASK-021 (install.sh must exist) and TASK-020 (rendered daemon service). Added to tasks index.

## [2026-06-12] task | TASK-021 Write install.sh — bash-3.2-safe, idempotent curl|bash installer
Created TASK-021 (ROADMAP-002 Phase 1): a repo-root install.sh for `curl … | bash`. Whole body wrapped in main(){…}; main "$@" (truncated-download guard) and authored bash-3.2-safe (no declare -A / mapfile / ${var^^}) while it checks for a PATH bash ≥4 the runtime scripts need. Ten ordered sections: env-overridable settings (CONDUCTOR_HOME/REPO_URL/BRANCH/CONDUCTOR_PAIRING_CODE), prereq checks with remedies (git, tmux ≥3.0 with suffix-strip, node ≥22.12 + warn on backend's stricter ≥26 floor, npm, PATH-bash ≥4, jq hard; sqlite3/Xcode-CLT warn-only), clone-or-ff-only-pull (never clobber a dirty tree), npm install ×3 with node-gyp-failure grep + remedy, mkdir data + db:migrate (probe-and-warn-skip since the script isn't wired yet) + db:verify warn-only, ./install-hooks.sh, ln -sf conductor → ~/.local/bin, `bin/conductor daemon install` (TASK-020 owns rendering) + healthz poll over the unix socket, graceful pairing via /dev/tty (skips when non-interactive or the pair subcommand is absent until Phase 3), and a summary. Fully idempotent. Logs to ~/.local/share/tmux-conductor/install.log. DEPENDS ON TASK-020 and ROADMAP-001 Phase 1; BLOCKS TASK-022. Added to tasks index.

## [2026-06-12] uat | UAT-016 UAT: Make AddAgentForm project-aware and add ProjectList/AddProjectForm components
Generated UAT-016 for TASK-016 with 18 test cases. Auto-runnable backbone (14): `make typecheck` static gate plus static-content greps confirming the `Project` type + `listProjects`/`createProject`/`spawnAgentInProject` helpers on the correct `/api/projects` routes in lib/api.ts, the new `AddProjectForm`/`ProjectList` components + their single mounts in index.astro, and AddAgentForm's project select, `spawnAgentInProject` branch, and project-driven workdir prefill/disable. Live runtime behaviors (create-project→list, project-row spawn auto-names, AddAgentForm prefill) are 4 clearly-marked human/Playwright-verification tests (`/uat-auto` skips them). Verified typecheck passes clean (exit 0) at generation time. Set task uat link; added to UAT index.

## [2026-06-12] uat | UAT-016 passed (auto)
Ran UAT-016 headless: all 14 UAT-STATIC tests pass (`make typecheck` exit 0; static-content checks confirmed the `Project` type/helpers/routes, `AddProjectForm`/`ProjectList` + their single index.astro mounts, and AddAgentForm's project select / `spawnAgentInProject` branch / workdir prefill+disable). The 4 UAT-UI tests recorded as auto-judge human-verification fails (expected — require a live browser/backend). No stub indicators found. Task TASK-016 marked done; UAT-016 → passed. Both files moved to completed/; index rows removed; ROADMAP-001 Phase 4 TASK-016 checked off.

## [2026-06-12] uat | UAT-017 UAT: Frontend Schedules UI — ScheduleList + ScheduleForm with live schedule-fired updates
Generated UAT-017 for TASK-017 with 18 test cases. Auto-runnable backbone (14): `make typecheck` static gate plus static-content greps confirming the `Schedule`/`CreateScheduleInput` types + `listSchedules`/`createSchedule`/`deleteSchedule` helpers on the correct `/api/schedules` routes in lib/api.ts (GET/POST-JSON/DELETE), the new `ScheduleForm`/`ScheduleList` components (ScheduleList renders ScheduleForm internally), the `useSSEEvent('schedule-fired', …)` subscription with no direct `new EventSource`, the next-fire/last-fired derivation from `lastEnqueuedAt + intervalSeconds` (`due now`/`never`), the single `<ScheduleList client:load />` mount in index.astro, and the backend `/api/schedules` routes the helpers target. Live runtime behaviors (create→appears, delete→removes, fire→flashes via SSE, form validation) are 4 clearly-marked human/Playwright-verification tests (`/uat-auto` skips them). Verified `make typecheck` passes clean (exit 0) at generation time. Set task uat link; added to UAT index.

## [2026-06-12] uat | UAT-020 UAT: Sync bin/conductor daemon install to the rendered-template + bootout/bootstrap flow
Generated UAT-020 for TASK-020 with 13 shell-assertable test cases covering the rewritten `daemon install` subcommand: 9 CLI/static tests (`bash -n` syntax gate; zero `launchctl load` remnants; all three placeholders referenced in sed exprs; `launchctl bootout`+`bootstrap` present; bootout tolerated with `|| true`; `uname -s`/`Darwin)`/`systemctl --user` OS branch; Linux `enable --now` + `loginctl enable-linger` tip; node-not-found error guard; `daemon help` text mentions launchd/systemd install), 3 dry-render tests (plist and .service.in rendered into ./tmp/ leave zero `__…__` placeholders; rendered LOG_PATH resolves to $HOME/.local/share/tmux-conductor/daemon.log), and 1 edge case (single-source constraint — no rendering sed duplicated outside bin/conductor). Contracts researched from bin/conductor install) block (lines 139-169), com.tmux-conductor.daemon.plist, and daemon/tmux-conductor-daemon.service.in. Set task uat link; added to UAT index.

## [2026-06-12] uat | UAT-019 UAT: Parameterize daemon launchd plist (log path, node path) and add a systemd user-unit template
Generated UAT-019 for TASK-019 with 14 fully-automatable shell-assertion test cases covering the two template files: 11 core tests (plist contains all three placeholders, zero /Users/davidtaylor literals, __LOG_PATH__ appears exactly twice in the two log-path string keys, PATH prepends __NODE_PATH__ ahead of the four system dirs; .service.in exists, carries all three placeholders, zero /Users/davidtaylor literals, has Restart=always, ExecStart mirrors the plist tsx run command, append:__LOG_PATH__ on both stdout+stderr, WantedBy=default.target) and 3 edge cases (rendered plist passes plutil -lint as valid XML, no .nvm/versions/node version path baked into either file, .service.in PATH prepends __NODE_PATH__ before the Linux system dirs with /opt/homebrew/bin omitted). systemd-analyze verify deferred to TASK-020 per the source task's out-of-scope note. All assertions executed against the live files during generation. Task frontmatter already linked the UAT path; added to UAT index between UAT-017 and UAT-020.

## [2026-06-12] uat | UAT-021 UAT: Write install.sh — bash-3.2-safe, idempotent curl|bash installer
Generated UAT-021 for TASK-021 with 19 test cases covering the curl|bash installer: 16 auto-runnable shell-assertion STATIC tests (`bash -n` syntax gate; no bash-4-only constructs declare -A/mapfile/readarray/${var^^}/${var,,} outside the doc comment; `main(){…}; main "$@"` truncated-download guard with the trailing line check; `set -euo pipefail` header; delegates to `bin/conductor daemon install` with zero plist/systemd rendering markers; pairing reads `/dev/tty` not stdin with the `[ -r /dev/tty ]` guard; prereq checks reference git/tmux/node/npm/jq via `command -v`; PATH-bash≥4 check inspects `command -v bash --version` and never `$BASH_VERSION`; `ln -sf` idempotent symlink; `merge --ff-only` + `status --porcelain` dirty-tree guard; healthz poll over the unix socket; defensive db:migrate/db:verify scripts[] probe + warn-skip; graceful pairing skip when `pair --help` absent; 10 `Section N —` markers in ascending order; XDG-data-dir install.log; the five CONDUCTOR_* env-overridable settings) plus 3 MANUAL/scratch-home LIVE tests (clean install into a scratch $HOME via `file://$PWD` clone, idempotent non-destructive re-run, dirty-tree warn-skip). Contracts researched from install.sh (all 477 lines) and the frontend/backend package.json engines.node floors (22.12.0 / 26.0.0). Set task uat link; added to UAT index.

## [2026-06-12] uat | UAT-019 passed (auto)
Task TASK-019 marked done. All 14 shell-assertion tests passed headless: plist carries all three placeholders, zero /Users/davidtaylor literals, __LOG_PATH__ exactly twice, PATH prepends __NODE_PATH__; .service.in exists with all three placeholders, zero home literals, Restart=always, ExecStart mirrors the plist tsx run command, append:__LOG_PATH__ on both streams, WantedBy=default.target; rendered plist passes plutil -lint, no .nvm/versions/node path in either file, .service.in PATH prepends __NODE_PATH__ before the Linux system dirs. (UAT-API-001/006 token-presence assertions rewritten to the clean per-token form — the generated xargs chain mishandled grep -lZ's null terminator on macOS; verified intent unchanged.) UAT-019 → passed, TASK-019 → done; both moved to completed/; index rows removed; ROADMAP-002 Phase 1 TASK-019 checkbox flipped.

## [2026-06-12] uat | UAT-018 UAT: Group AgentList by project and react to the new ID-based task SSE events
Generated UAT-018 for TASK-018 with 13 test cases covering the frontend AgentList project-grouping rework: 5 API tests (GET /api/agents returns ApiAgent rows with projectId; GET /api/projects returns id→name rows; POST /api/tasks → 201 full Task + task-added SSE; PUT /api/tasks/reorder → queue-reordered; POST /api/tasks/:id/jump-head → task-moved), 5 UI tests (one section per non-empty project with Unassigned last and alphabetical ordering; per-section agent-count badges matching rendered cards; status BOARD_COLUMNS nested per project with no cross-group accordion bleed via composite group:column keys; task-added live-refreshes queued-task count without reload and with no console error; a brand-new agent lands in its assigned project group live), 2 edge cases (membership fetch failure degrades to all-Unassigned without the red error banner; malformed task SSE swallowed by try/catch keeping the stream open) and 1 static gate (make typecheck clean). Contracts researched from frontend AgentList.tsx (loadMembership + 4 task listeners), lib/api.ts (fetchAgents/listProjects/ApiAgent/Project/Task), backend/index.ts task routes (broadcastSSE emitters) and db.ts listAgents/listProjects. Set task uat link; added to UAT index.

## [2026-06-12] roadmap | Merge .docs/roadmaps/ into wiki/work/roadmaps/
Created ROADMAP-003 (done — Local Agents Dashboard, migrated from .docs/001), ROADMAP-004 (done — Backend/Frontend Restructure to TypeScript, migrated from .docs/004), ROADMAP-005 (done/superseded — Ngrok-Like Tunnel Daemon, superseded by ROADMAP-002 relay), ROADMAP-006 (done/superseded — oauth2-proxy DO App Platform deploy, superseded by ROADMAP-002 OIDC portal; Phase 1 bootstrap artefacts remain in repo). Added Prior Art section to ROADMAP-002 cross-linking ROADMAP-005 and ROADMAP-006. No changes to .docs/roadmaps/ files.

## [2026-06-12] uat | UAT-020 passed (auto)
Task TASK-020 marked done. All 12 shell-assertion tests passed headless: `bash -n bin/conductor` clean; deprecated `launchctl load` fully removed (count 0); all three placeholders `__REPO_ROOT__`/`__LOG_PATH__`/`__NODE_PATH__` referenced in the install sed; macOS reload uses `launchctl bootout … || true` + `launchctl bootstrap`; OS branch on `uname -s` with a `Darwin)` case and `systemctl --user` daemon-reload + `enable --now tmux-conductor-daemon.service` plus the `loginctl enable-linger` tip; node-not-found guard prints "Error: node not found on PATH"; `daemon help` describes install as launchd/systemd; both dry-renders (plist + .service.in) into ./tmp left zero surviving placeholders; rendered LOG_PATH resolves to `$HOME/.local/share/tmux-conductor/daemon.log`; no rendering sed duplicated outside bin/conductor. UAT-020 → passed, TASK-020 → done; both moved to completed/; index rows removed; ROADMAP-002 Phase 1 TASK-020 checkbox flipped.

## [2026-06-12] uat | UAT-017 passed (auto)
Task TASK-017 marked done. All 14 UAT-STATIC tests passed headless: `make typecheck` exit 0; static-content checks confirmed the `Schedule`/`CreateScheduleInput` types + `listSchedules`/`createSchedule`/`deleteSchedule` helpers on the correct `/api/schedules` routes (GET/POST-JSON/DELETE), the `ScheduleForm`/`ScheduleList` components (ScheduleList renders ScheduleForm internally), the `useSSEEvent('schedule-fired', …)` subscription with no direct `new EventSource`, the next-fire/last-fired derivation from `lastEnqueuedAt + intervalSeconds` (`due now`/`never`), the single `<ScheduleList client:load />` mount in index.astro, and the backend `/api/schedules` GET/POST/DELETE routes. The 4 UAT-UI tests recorded as auto-judge human-verification fails (expected — require a live browser/backend). No stub indicators found. UAT-017 → passed, TASK-017 → done; both moved to completed/; index rows removed; ROADMAP-001 Phase 4 TASK-017 checkbox flipped.

## [2026-06-12] uat | UAT-021 passed (auto)
Task TASK-021 marked done. All 16 UAT-STATIC shell-assertion tests passed headless: `bash -n install.sh` clean; zero functional bash-4 constructs (declare -A/mapfile/readarray/${var^^}); `main(){…}` truncated-download wrapper with trailing `main "$@"`; `set -euo pipefail` header; delegates to `bin/conductor daemon install` with no re-implemented plist rendering; pairing reads `/dev/tty` with readability guard; prereq `command -v` checks for git/tmux/node/npm/jq; PATH-bash `--version` gate (not `$BASH_VERSION`); idempotent `ln -sf` symlink; `merge --ff-only` + `status --porcelain` dirty-tree guard; healthz poll over `--unix-socket "$SOCK"`; defensive db:migrate/db:verify warn-skip; graceful `pair --help` degrade; 10 ordered `Section N —` headers; XDG `install.log` logging; five `${CONDUCTOR_*:-default}` env overrides. The 3 UAT-LIVE tests (clean install, idempotent re-run, dirty-tree warn-skip) recorded as manual/scratch-home human-verification required (expected — heavy live install run, do not block). UAT-021 → passed, TASK-021 → done; both moved to completed/; index rows removed; ROADMAP-002 Phase 1 TASK-021 checkbox flipped.

## [2026-06-12] uat | UAT-022 Verify install.sh end-to-end into a scratch CONDUCTOR_HOME on macOS
Generated UAT-022 for TASK-022 with 7 test cases: 5 auto-runnable shell assertions (UAT-SHELL-001 `bash -n` on install.sh + bin/conductor; UAT-SHELL-002 sandboxed plist render into ./tmp with zero residual placeholders + `plutil -lint` OK; UAT-SHELL-003 ff-only update guard present and no `reset --hard` re-clone; UAT-SHELL-004 daemon step delegates to `bin/conductor daemon install` with launchctl bootout/bootstrap in the CLI; UAT-SHELL-005 non-interactive pairing skip branches) plus 2 MANUAL human-verification tests (UAT-MANUAL-001 live launchctl bootstrap, UAT-MANUAL-002 live `/healthz` over the socket) flagged as throwaway-$HOME/CI-runner only and EXPECTED non-blocking fails under headless auto-run because install.sh + `bin/conductor daemon install` hardcode the daemon identity to $HOME.

## [2026-06-12] uat | UAT-018 passed (auto)
Task TASK-018 marked done. Headless /uat-auto ran all 13 tests against the live backend (8788) + frontend (4321): UAT-API-001/002 (GET /api/agents + /api/projects shapes carrying projectId / id→name), UAT-API-004 (PUT /tasks/reorder → 200 {ok:true} + queue-reordered), UAT-API-005 (POST /tasks/:id/jump-head → 200 {ok,id} + task-moved), and UAT-STATIC-001 (`make typecheck` exit 0, both packages) all PASS. Stub-detection gate clean — AgentList.tsx fully implements per-project grouping (Unassigned last), count badges, composite group:column accordion keying, loadMembership() re-fetch on new agent + task-added, and all four task SSE listeners in try/catch. UAT-UI-001..005 recorded as UI human-verification required and UAT-EDGE-001/002 as manual human-verification required (expected non-blocking). UAT-API-003 is the one substantive FAIL: the /uat-generate curl sends `agentId` but POST /api/tasks reads `agentName` (backend/index.ts:365) so the response echoed agentId:null vs the expected sent id — a test-authoring bug, not a feature/backend defect (verified: passing `agentName` returns the resolved agentId:6). Per task override (UI/manual fails non-blocking, no stubs), housekeeping completed: UAT-018 → passed, TASK-018 → done, both moved to completed/, index rows removed, ROADMAP-001 TASK-018 checkbox flipped with completed-2026-06-12 + completed/ link.

## [2026-06-12] uat | UAT-022 passed (auto, scoped)
Task TASK-022 marked done. All 5 auto-runnable UAT-SHELL tests passed headless: UAT-SHELL-001 `bash -n install.sh` + `bash -n bin/conductor` clean; UAT-SHELL-002 sandboxed plist render into ./tmp left zero residual `__…__` placeholders, `plutil -lint` OK, Label `com.tmux-conductor.daemon`; UAT-SHELL-003 install.sh has the `merge --ff-only` update guard + dirty-tree skip and no `reset --hard` destructive re-clone; UAT-SHELL-004 daemon step delegates to `"$INSTALL_DIR/bin/conductor" daemon install` with `launchctl bootout`/`bootstrap` living in bin/conductor (verified with fixed-string `-F` grep — `$(id -u)` carries regex metacharacters; test body updated to `-F`); UAT-SHELL-005 both non-interactive pairing skip branches present. The 2 MANUAL tests (UAT-MANUAL-001 live launchctl bootstrap, UAT-MANUAL-002 live `/healthz` over the socket) recorded as auto-judge human-verification-required (EXPECTED non-blocking — require throwaway-$HOME/CI runner because install.sh + `bin/conductor daemon install` hardcode the daemon identity to $HOME). UAT-022 → passed, TASK-022 → done; both moved to completed/; index rows removed; ROADMAP-002 Phase 1 TASK-022 checkbox flipped with completed-2026-06-12 + completed/ link.

## [2026-06-12] task | TASK-023 Delete dead conf-splice + legacy file-queue code from backend/config.ts and backend/state.ts
Created TASK-023 (ROADMAP-001 Phase 5 Cutover). Serena-grounded research found the deletion is only partly dead-code: `appendAgentToConf`/`removeAgentFromConf` have zero callers (delete), but the BG-process conf-splice fns (`appendBgProcessToConf`/`removeBgProcessFromConf`/`addBgLink`/`removeBgLink`) are still live in the `/bg-processes` routes (bg processes stay conf-backed — keep). The state.ts file-queue fns (`countQueuedTasks`/`readQueue`/`writeQueue`/`getAgentLines` + `writeChain`) are still called by `backend/index.ts`, so steps migrate the two `countQueuedTasks` callers to `listTasksForAgent(db,name).length`, delete the superseded flat-file `POST/PUT/DELETE /queue/:agent*` routes (DB `/api/tasks` is the live replacement; `GET /queue/:agent` already DB-backed stays), prune the unused imports, then delete the orphaned fns. Gate on `make typecheck`. depends_on []; blocks TASK-024. Added to tasks index.

## [2026-06-12] task | TASK-024 Postgres migration 001 — users, devices, pairing_codes
Created task TASK-024: author portal/migrations/001_init.sql (pgcrypto extension; users/devices/pairing_codes identity tables with SHA-256 hashed token_hash/code_hash bytea columns and a partial active-device index) applied by the TASK-023 migrate.ts runner. DEPENDS ON TASK-023; BLOCKS TASK-025 (OIDC user upserts). ROADMAP-002 Phase 2, line 32.

## [2026-06-12] task | TASK-026 Run the end-to-end SQLite-migration verification suite
Created TASK-026 (ROADMAP-001 Phase 5 Cutover, FINAL task). Serena-grounded against the real atomic-pop SQL (`scripts/lib/db.sh` `pop_task_sql` + `backend/db.ts` `popTask`), the monitor dispatch path (`pop_task → emit_dispatch_jsonl → mark_busy → dispatch`, dead-pane `move_to_backlog`), the 5s scheduler tick (`dueSchedules → fireSchedule` with `skip_if_pending` + `action 'jump'→'head'`, `broadcastSSE('schedule-fired'/'task-added')`), and `restoreBacklog` (`UPDATE tasks SET status='queued'`, positions preserved). Specifies a single runnable `scripts/verify-sqlite-migration.sh` against a disposable `./tmp/verify/` DB (never the live `data/conductor.db`), packaging the plan's six verification items into the five required fail-closed checks each with an explicit machine-checkable pass criterion: (1) seed import — 6 tables + `meta.legacy_import=1` + no-duplicate restart; (2) atomic pop race — 500 tasks, zero `uniq -d` duplicates, queue drains, plus scoped→project→global precedence; (3) fake-agent dispatch — `bash`-launch agent, `echo POPPED-OK` capture-pane + row deleted + dispatch.jsonl record; (4) schedule fire — SSE `schedule-fired` + `source='schedule'` row, `skip_if_pending` holds count at 1, `jump` lands at head; (5) backlog restore — scoped rows flip to backlog with positions preserved then restore to queued and dispatch. depends_on [TASK-023, TASK-024, TASK-025]. Added to tasks index.

## [2026-06-12] task | TASK-027 Scaffold portal/ (Fastify, env validation, pg Pool, boot-time migrations) with /healthz
Created the ROADMAP-002 Phase 2 portal boot-skeleton task. Requested as TASK-023, but a concurrent agent had already claimed id TASK-023 (the ROADMAP-001 Phase 5 conf-queue cleanup) and TASK-024/025/026 were likewise taken by that series — so renumbered to TASK-027 to keep stable IDs unique (file `wiki/work/tasks/TASK-027-scaffold-portal-foundation.md`, header note records the rename). NOTE on cross-series collision: the concurrently-created portal-migration task (id TASK-024, "001_init.sql") references "the TASK-023 migrate.ts runner" — i.e. it expects this scaffold to live at TASK-023; that link is now stale and points at the wrong id (TASK-023 is the conf-queue task). Human reconciliation needed: either renumber the conf-queue task or repoint the portal-migration task's prose to TASK-027. Scope: new top-level `portal/` dir mirroring backend's ESM + Fastify 5 + tsx conventions — `package.json`/`tsconfig.json`/`env.ts` (tiered fail-fast validation: DATABASE_URL hard-required, SESSION_SECRET ≥32B when present, Google/session vars warn-and-continue, ALLOWLIST_EMAILS comma-split, PORT default 8080)/`db.ts` (pg Pool singleton, DO self-signed `ssl:{rejectUnauthorized:false}` + prod-CA TODO)/`migrate.ts` (pg_advisory_lock, schema_migrations table, ordered NNN_*.sql in txns, idempotent, tolerates zero files)/`index.ts` (Fastify boot, liveness-only `GET /healthz` → `{ok:true}`, listen 0.0.0.0:PORT) + `migrations/.gitkeep`. Auth/relay/device-pairing/Dockerfile.portal/do-app.yaml/001_init.sql DDL explicitly out of scope. depends_on []; blocks left empty (intended portal-series numbers taken — back-link fresh portal tasks later). Added to tasks index.

## [2026-06-12] task | TASK-025 Portal Google OIDC sign-in, JWT session cookie, email allowlist, /api/me
Created TASK-025 (ROADMAP-002 Phase 2, implementation-plan Workstreams A3 Auth + A5 routes). Adds first-party Google OpenID Connect sign-in to the hosted `portal/` app using `openid-client` v6 (discovery + PKCE + state/nonce + ID-token validation built in — `@fastify/oauth2` explicitly rejected) plus `jose` (HS256 session signing) and `@fastify/cookie`. Files: `portal/auth/oidc.ts` (memoized Google discovery, buildAuthRedirect, handleCallback, signed short-lived `tc_oidc` flow cookie), `portal/auth/session.ts` (stateless HS256 `tc_session` JWT — HttpOnly/Secure/SameSite=Lax/Path=/, 7-day expiry, signed with SESSION_SECRET, NO server-side session store), `portal/auth/allowlist.ts` (parse comma-separated ALLOWLIST_EMAILS, case-insensitive isAllowed, fail-closed on empty), `portal/auth/guards.ts` (Origin/Sec-Fetch-Site CSRF check on ALL non-GET routes + requireSession + requireAllowed preHandlers). Routes: `GET /auth/google` (start flow), `GET /auth/google/callback` (validate state/nonce/ID-token, REQUIRE email_verified===true, upsert on google_sub updating email/name/picture/last_login_at, snapshot allowlist decision into users.is_allowed, set session cookie, redirect /), `POST /auth/logout` (clear cookie), and `GET /api/me` (requireSession only, NOT requireAllowed → `{user:{id,email,name,picture}, isAllowed}`, so non-allowlisted signed-in users get isAllowed:false to drive the request-access UI). The Google Cloud Console OAuth client + redirect URI is a human deploy-time step noted as non-blocking; all code is testable via a mock/quiet OIDC path. depends_on [TASK-027 (portal scaffold — the item the plan called "TASK-023", renumbered), TASK-024 (users table)]; blocks [TASK-026]. Back-linked into TASK-027.blocks. Added to tasks index.

## [2026-06-12] task | TASK-024 Strip AGENTS/BG_PROCESSES/AGENT_BG_LINKS/TASK_QUEUE from conductor.conf and retire tasks.txt
Created TASK-024 (ROADMAP-001 Phase 5 Cutover, conf-side half). Serena-grounded research confirmed `backend/config.ts` is the sole blocker — it still `declare -p`-parses all four keys and text-splices AGENTS/BG/link blocks back into the conf (TASK-023 removes that). The runtime already reads SQLite (`scripts/lib/db.sh` `load_agents`/`load_bg`/`pop_task_sql`), so stripping the conf blocks is inert once TASK-023 lands. Steps: pre-flight gate that the parsing is gone; delete the AGENTS/BG_PROCESSES/AGENT_BG_LINKS arrays + TASK_QUEUE line + their comment headers from conductor.conf (keeping CLAUDE_FLAGS/CLEAR_CMD + all tuning keys, just rewording array-specific comments); remove the `$TASK_QUEUE` path-resolution + tasks.backlog.txt restore block + Queue echo from scripts/conductor.sh and scripts/spawn.sh; drop the `./tasks.txt:/app/tasks.txt` docker mount; remove the tasks.txt/tasks.backlog.txt import branch from backend/db.ts `seedFromLegacy()` (+ orphaned `importTaskLines`); `git rm tasks.txt tasks.backlog.txt`; sync live docs (README/CONDUCTOR/ELEVATOR_PITCH/flowchart/SCRIPTS_GLOSSARY/CLAUDE off the tasks.txt + conf-array model); verify with bash -n + make typecheck + zero-reference greps. depends_on [TASK-023]. CROSS-SERIES COLLISION: a concurrent ROADMAP-002 agent already holds id TASK-024 (`TASK-024-portal-pg-migration-001.md`); both rows now sit in the index under 024 — orchestrator must reconcile the duplicate numbering. OVERLAP: the concurrently-created TASK-025 ("Update docker-compose mounts, Dockerfile native-build step, and docs for the SQLite data layer") covers the same docker-compose tasks.txt mount and README/CLAUDE doc edits — these two tasks must be de-duplicated/sequenced by the orchestrator to avoid double-editing docker-compose.yml and the docs. Added to tasks index.

## [2026-06-12] task | TASK-028 Dockerfile.portal + deploy/do-app.yaml (DO App Platform spec)
Created the ROADMAP-002 Phase 2 deployment-artefacts task. Requested as TASK-026 (slug portal-dockerfile-do-app-deploy) with dependency TASK-023, but the live tree showed TASK-023/024/025/026/027 already claimed by concurrent agents (ROADMAP-001 Phase 5 cutover series + the renumbered portal scaffold), and TASK-026 specifically was taken by a different task (e2e-verification-suite) — so per the task-add re-verify-number rule this was bumped to the next free id TASK-028 (file `wiki/work/tasks/TASK-028-portal-dockerfile-do-app-deploy.md`, header note records the rename). The intended dependency "Scaffold portal/ … with /healthz" landed as TASK-027 (itself a renumber of the originally-requested TASK-023), so depends_on points at TASK-027. Scope: author ONLY the files — (1) a multi-stage repo-root `Dockerfile.portal` (stage 1 builds the frontend with `PUBLIC_PORTAL_MODE=1` into frontend/dist; final stage node:22-alpine, `npm ci --omit=dev` portal deps, copies portal/ + built frontend into portal/ui/dist, ENV PORT=8080 / EXPOSE 8080 / CMD runs the portal) leaving the existing `Dockerfile.prod` UNTOUCHED; (2) `deploy/do-app.yaml` (one single-instance `portal` service — instance_count:1 with the in-memory-relay-registry-forbids-scaling comment — dev Postgres production:false, Dockerfile-based build at Dockerfile.portal, http_port 8080, /healthz check, envs GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET/SESSION_SECRET as SECRET, ALLOWLIST_EMAILS, DATABASE_URL ${db.DATABASE_URL}, PUBLIC_BASE_URL ${APP_URL}, PORT 8080); (3) `deploy/README.md` runbook. The live `doctl apps create` deploy and the Google Cloud OAuth client + redirect-URI creation are explicit DEFERRED manual Acceptance Criteria flagged "requires DigitalOcean account + Google Cloud Console" — this autonomous run does not run doctl or create cloud resources. depends_on [TASK-027]. Added to tasks index.

## [2026-06-13] task | TASK-031 Portal pairing-code creation API + rate-limited /pair/redeem
Created TASK-031: portal Phase 3 pairing API — POST /api/pair/code (8-char Crockford base32, ≤5 outstanding per user, SHA-256 hashed in pairing_codes, 10-min expiry) and POST /api/pair/redeem (atomic single-use redemption, creates devices row with hashed token, returns one-time tcd_… token). Depends on TASK-027 (scaffold), TASK-029 (migration), TASK-030 (auth guards). ROADMAP-002 Phase 3.

## [2026-06-13] uat | UAT-023 UAT: Delete dead conf-splice + legacy file-queue code from backend/config.ts and backend/state.ts
Generated UAT-023 for TASK-023 with 13 test cases covering the dead-code deletion: 8 static grep gates (typecheck clean, 6 deleted symbols absent from backend/, 3 legacy mutating routes absent, GET /queue/:agent kept, state.ts import pruned, fsPromises removed, config.ts import pruned, BG-process helpers still present), 5 API tests (GET /status queuedTasks is a numeric DB-backed field; POST/PUT/DELETE /queue/:agent* return 404; GET /queue/:agent still returns task objects; GET /status bgProcesses non-crashing regression check), and 1 edge-case (writeChain variable also absent).

## [2026-06-13] task | TASK-034 shared/relay-protocol.ts frame contract — done
Created `shared/relay-protocol.ts` with all six discriminated-union frame types (`RelayRequestFrame`, `RelayResponseHeadFrame`, `RelayBodyChunkFrame`, `RelayResponseEndFrame`, `RelayCancelFrame`, `RelayErrorFrame`), union aliases (`RelayFrame`, `InboundRelayFrame`, `OutboundRelayFrame`), and guard functions (`isRelayFrame`, `isInboundRelayFrame`). Pure TypeScript — no imports, no runtime dependencies. Standalone `tsc --noEmit --strict` passes. tsconfig paths wiring deferred to TASK-035 (portal/ not yet created). TASK-034 → done; removed from tasks index.

## [2026-06-13] uat | UAT-034 UAT: shared/relay-protocol.ts frame contract (req/res/body chunks/cancel/err, correlation ids)
Generated UAT-034 for TASK-034 with 20 test cases covering the pure TypeScript relay protocol module: 13 static grep/compile tests (file presence + warning comment, zero import statements, all 6 frame type exports, 3 union alias exports, 2 guard function exports, exact type discriminator strings, RelayRequestFrame/RelayResponseHeadFrame/RelayBodyChunkFrame/RelayErrorFrame field shapes, InboundRelayFrame/OutboundRelayFrame member sets, standalone strict tsc gate) and 7 runtime tests via npx tsx (isRelayFrame happy path for relay:request, all 6 frame types return true, null/missing-correlationId/unknown-type all return false, isInboundRelayFrame inbound=true/outbound=false for all 6 types, non-object primitive rejection, non-string correlationId rejection, non-UUID string accepted as CorrelationId). All runtime assertions executed and verified during generation.

## [2026-06-13] uat | UAT-027 UAT: Scaffold portal/ (Fastify, env validation, pg Pool, boot-time migrations) with /healthz
Generated UAT-027 for TASK-027 with 12 test cases covering the portal scaffold: 2 static checks (file structure presence + 001_init.sql absence, tsc --noEmit zero-error), 3 env-validation tests (missing DATABASE_URL exits non-zero with collected error; SESSION_SECRET <32 bytes exits non-zero; all auth vars absent yields warning-only with server continuing), 2 API tests (GET /healthz returns HTTP 200 {"ok":true}; unknown path returns 404), 3 migration tests (zero-file run logs "no migrations to apply" and exits 0; schema_migrations table created; second run is a no-op), and 2 edge-case tests (unreachable DATABASE_URL surfaces clear error and exits; PORT env var respected; invalid PORT exits non-zero).

## [2026-06-13] uat | UAT-023 passed (auto)
Task TASK-023 marked done. All 15 tests passed: 8 static grep gates (typecheck clean, 6 deleted symbols absent from backend/, 3 legacy mutating routes absent, GET /queue/:agent kept, state.ts import pruned, fsPromises removed, config.ts import pruned, BG-process helpers still present), 5 API tests (queuedTasks is numeric/DB-backed, POST/PUT/DELETE /queue/:agent* return 404, GET /queue/:agent returns task objects, GET /status bgProcesses regression clean), and 2 edge-case tests (writeChain absent, live BG-process routes functioning). UAT-023 → passed, TASK-023 → done. Both moved to completed/; UAT-023 row removed from UAT index; ROADMAP-001 Phase 5 TASK-023 checkbox flipped.

## [2026-06-13] uat | UAT-027 passed (auto)
Task TASK-027 marked done. All 13 tests passed: UAT-STATIC-001 (all 7 scaffold files present, 001_init.sql absent), UAT-STATIC-002 (tsc --noEmit exit 0), UAT-ENV-001 (missing DATABASE_URL → collected error + exit 1), UAT-ENV-002 (SESSION_SECRET too short → collected error + exit 1), UAT-ENV-003 (only DATABASE_URL set → warning + server starts, /healthz returns {"ok":true}), UAT-API-001 (GET /healthz → 200 {"ok":true}), UAT-API-002 (GET /notfound → 404 Fastify error object), UAT-MIGRATE-001 (npm run migrate with zero .sql files → "no migrations to apply" + exit 0), UAT-MIGRATE-002 (schema_migrations table created with version/applied_at columns), UAT-MIGRATE-003 (second migrate run is a no-op), UAT-EDGE-001 (unreachable DATABASE_URL → clear migration error + exit 1), UAT-EDGE-002 (PORT=9090 respected → /healthz on 9090 returns {"ok":true}), UAT-EDGE-003 (invalid PORT → collected error + exit 1). UAT-027 → passed, TASK-027 → done; removed from active indexes; ROADMAP-002 Phase 2 TASK-027 checkbox flipped.

## [2026-06-13] uat | UAT-034 passed (auto)
Task TASK-034 marked done. All 20 tests passed: static checks (file presence + wire-protocol comment, zero import statements, all 6 frame type exports, 3 union aliases, 2 guard functions, exact type discriminators, all per-frame field shapes, InboundRelayFrame/OutboundRelayFrame membership, standalone strict tsc clean) and runtime checks via npx tsx (isRelayFrame accept/reject scenarios, isInboundRelayFrame inbound/outbound narrowing, edge-case non-object/non-string-correlationId rejection, non-UUID string accepted). UAT-STATIC-003 test command corrected from overly-broad `export type Relay` pattern to `export type Relay[A-Z][a-zA-Z]*Frame\b` to exclude the union alias. UAT-034 → passed, TASK-034 → done; both moved to completed/; UAT-034 row removed from UAT index; ROADMAP-002 Phase 4 TASK-034 checkbox already checked.

## [2026-06-13] task | TASK-024 Strip AGENTS/TASK_QUEUE from conductor.conf and retire tasks.txt — done
Completed TASK-024. Stripped `AGENTS=(…)` and `TASK_QUEUE=` from `conductor.conf` (left `BG_PROCESSES`/`AGENT_BG_LINKS` intact — still live for /bg-processes routes). Removed tasks.txt backlog-restore block + TASK_QUEUE case lines from `scripts/conductor.sh` and `scripts/spawn.sh`; replaced queue-count echo with SQLite count. Dropped `./tasks.txt:/app/tasks.txt` docker mount (replaced with `./data:/app/data`). Removed tasks.txt/tasks.backlog.txt import branch from `backend/db.ts` `seedFromLegacy()` and deleted orphaned `importTaskLines`. Removed `AGENTS`/`TASK_QUEUE` from `declare -p` in `backend/config.ts`. Deleted `tasks.txt` and `tasks.backlog.txt` via `git rm`. Updated live docs: README.md, CONDUCTOR.md, ELEVATOR_PITCH.md, conductor-workflow.flowchart.md, SCRIPTS_GLOSSARY.md, CLAUDE.md, scripts/README.md. All static gates pass: `bash -n` clean on all three scripts, `make typecheck` zero errors, zero `tasks\.txt|AGENTS=` refs outside historical docs.

## [2026-06-13] uat | UAT-026 UAT: End-to-end SQLite migration verification suite
Generated UAT-026 for TASK-026 with 8 test cases: 1 script-existence check, 1 full-suite green-run check, 5 per-check behavioral assertions (seed schema init, atomic pop + precedence, fake dispatch, schedule fire + skip_if_pending + jump, backlog restore + redispatch), and 2 isolation checks (idempotency, real DB untouched).

## [2026-06-13] uat | UAT-026 passed
Task TASK-026 marked done. Verified the end-to-end SQLite migration suite — all 5 checks (seed-import, atomic pop + precedence, fake dispatch, schedule fire + skip_if_pending + jump, backlog restore) passed via `scripts/verify-sqlite-migration.sh`, confirmed idempotent on second run, real DB untouched.

## [2026-06-13] uat | UAT-025 UAT: Update docker-compose mounts, Dockerfile native-build step, and docs for the SQLite data layer
Generated UAT-025 for TASK-025 with 12 test cases: 5 infra tests (compose volume mounts, 3× Dockerfile native-build toolchain), 6 doc tests (absence of old flat-file references + SQLite presence in README.md, CLAUDE.md, scripts/README.md), and 2 edge tests (frontend Dockerfile and dev-compose untouched).

## [2026-06-13] uat | UAT-025 passed (auto)
Task TASK-025 marked done. All 13 tests passed: UAT-INFRA-001 (./data:/app/data mount present in docker-compose.yml), UAT-INFRA-002 (tasks.txt mount absent), UAT-INFRA-003 (Dockerfile.prod server-deps stage wraps npm install --omit=dev with virtual .build-deps toolchain), UAT-INFRA-004 (backend/Dockerfile.prod builder stage wraps npm install --omit=dev), UAT-INFRA-005 (backend/Dockerfile.dev wraps npm install without --omit=dev), UAT-DOC-001 (README.md has no tasks.txt/TASK_QUEUE/BG_PROCESSES=/AGENTS= refs), UAT-DOC-002 (README.md references conductor.db and tasks table), UAT-DOC-003 (CLAUDE.md has no old flat-file refs), UAT-DOC-004 (CLAUDE.md has tuning settings only phrase), UAT-DOC-005 (scripts/README.md has no old flat-file refs), UAT-DOC-006 (scripts/README.md references SQLite/conductor.db), UAT-EDGE-001 (frontend/Dockerfile.dev has no .build-deps toolchain), UAT-EDGE-002 (docker-compose.build.yml has no ./data or tasks.txt refs). UAT-025 → passed; TASK-025 already done; UAT-025 row removed from UAT index. ROADMAP-001 is now fully complete — all 19 items checked; ROADMAP-001 → done; removed from roadmaps index.

## [2026-06-12] task | TASK-025 Update docker-compose mounts, Dockerfile native-build step, and docs for the SQLite data layer
Created TASK-025 (ROADMAP-001 Phase 5 Cutover, roadmap line 52). Id and filename `TASK-025-update-docker-dockerfile-docs.md` were explicitly reserved by the orchestrator (do-not-renumber). CROSS-SERIES ID COLLISION: a concurrent ROADMAP-002 agent also created a TASK-025 (`TASK-025-portal-google-oidc-session-allowlist.md`) in the same dir — two distinct TASK-025 files now coexist (unique slugs; index carries both rows). Orchestrator must reconcile the two numbering tracks. SCOPE OVERLAP with sibling TASK-024 (`TASK-024-strip-legacy-conf-keys-retire-tasks-txt.md`): that task's prose also claims the docker-compose tasks.txt-mount removal + README/CLAUDE doc sync. This task (line 52) is the canonical owner of the docker-compose mount changes + Dockerfile native-build step + docs rewrite; TASK-024 (line 51) owns conductor.conf key-stripping + tasks.txt file retirement. The two must be sequenced by the orchestrator so docker-compose.yml/README/CLAUDE aren't double-edited. Serena-grounded research: docker-compose.yml `dashboard` service still mounts obsolete `- ./tasks.txt:/app/tasks.txt` with NO `./data` mount (DB gitignored at .gitignore:220 → must be a host volume to persist); both prod images (`Dockerfile.prod` stage 2 `server-deps`, `backend/Dockerfile.prod` stage 1 `builder`) + `backend/Dockerfile.dev` run a bare `npm install` on `node:22-alpine` with NO toolchain — `better-sqlite3` ^12.0.0 (confirmed prod dep) is glibc-prebuilt-only and must compile from source on musl, so the native module currently can't build. Deliverables: (1) compose — drop tasks.txt mount, add `- ./data:/app/data` (rw; service runs `${UID}:${GID}`); (2) wrap each install stage with `apk add --no-cache --virtual .build-deps python3 make g++ && npm install … && apk del .build-deps`; (3) rewrite README.md (~13/27/44/49/190/218/297/301-307 + mermaid queue node + "No database" section), CLAUDE.md (~9/13/23-29/33/51/52/55/56), scripts/README.md (flowchart node, monitor.sh/add-task.sh descriptions) off the tasks.txt + conf AGENTS/BG_PROCESSES/TASK_QUEUE model onto SQLite (DB-backed projects/schedules, conf-is-tuning-only). Validation gate: `docker build -f Dockerfile.prod` must compile better-sqlite3 on musl, else confirm toolchain lines present + note build not run. Collision-safe — disjoint file set from siblings: depends_on []; parallel_safe_with [TASK-023, TASK-024]; doc wording assumes 023/024 land. Did NOT edit the roadmap (orchestrator links it). Added to tasks index.
