# Bugs Archive

Terminal bug files (`closed`, `wontfix`, `duplicate`, or `cannot-reproduce`) moved here by `/wiki-archive` to reduce directory clutter. **Append-only** — archived items never move again.

| ID | Title | Final Status | Archived |
|----|-------|--------------|----------|
